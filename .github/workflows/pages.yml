# GitHub Pages deployment - Main website with README as homepage
name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - 'README.md'
      - 'index.md'
      - 'docs/leaderboard.html'
      - 'docs/querygym-logo.png'
      - '_config.yml'
      - 'Gemfile'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install dependencies
        run: bundle install

      - name: Build with Jekyll
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Copy additional files
        run: |
          # Copy leaderboard to _site
          cp docs/leaderboard.html _site/leaderboard.html
          # Copy logo for the site
          cp docs/querygym-logo.png _site/querygym-logo.png 2>/dev/null || true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
